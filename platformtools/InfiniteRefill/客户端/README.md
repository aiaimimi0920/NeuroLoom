# 本地客户端（curl + 中文命名）

本目录提供三类本地客户端脚本，全部使用 `curl` 调用服务器接口，并采用**中文文件名/中文提示**，面向小白。

对应设计：[`server/DESIGN.md`](../server/DESIGN.md:1)

---

## 0. 通用约定

- 服务器地址：例如 `https://xxx.workers.dev`
- 密钥放在 Header：
  - 普通用户：`X-User-Key: <USER_KEY>`
  - 热心群众：`X-Upload-Key: <UPLOAD_KEY>`（也可用于普通用户接口，等价于 `USER_KEY`）
  - 维修者：`X-Upload-Key: <UPLOAD_KEY>`（与热心群众同凭据体系，但主要调用 `/v1/repairs/*`）
  - 超级管理员：`Authorization: Bearer <ADMIN_TOKEN>`

### 0.1 JSON 解析依赖（macOS 小白不装 jq 也能跑）

- 本仓库的 macOS/Linux `.sh` 脚本现在**默认不再硬依赖** `jq`。
- 统一的 JSON 小工具见：[`客户端/_lib/json.sh`](_lib/json.sh:1)
- 优先级：`python3`（推荐） > `osascript/JXA`（macOS 自带） > `jq`（兜底）

### 0.2 Windows 依赖策略（小白零安装优先）

Windows 客户端默认只依赖系统内置组件：

- `cmd.exe`
- `PowerShell`
- `schtasks`

网络请求策略：

- 优先 `curl`（系统有则用）
- 若缺失 `curl`，自动回退到 `PowerShell Invoke-WebRequest`

同步策略（`SYNC_TARGET_DIR`）：

- 优先软链接同步
- 若软链接不可用/权限受限，自动回退为复制同步

因此对普通小白用户，**无需额外安装 Python/jq/git** 即可使用 Windows 版本。

---

## 1. 普通用户客户端（默认：无限续杯配置任务 + 自动清理）

目录：[`客户端/普通用户`](普通用户:1)

- 无限续杯（配置入口：定时续杯 + 也可单次触发）：
  - Windows：[`客户端/普通用户/无限续杯.bat`](普通用户/无限续杯.bat:1)
  - macOS/Linux：[`客户端/普通用户/无限续杯.sh`](普通用户/无限续杯.sh:1)

- 单次续杯（主动手动触发）：
  - Windows：[`客户端/普通用户/单次续杯.bat`](普通用户/单次续杯.bat:1)
  - macOS/Linux：[`客户端/普通用户/单次续杯.sh`](普通用户/单次续杯.sh:1)

- 无限续杯与自动清理的“定时配置”都已整合进 [`客户端/普通用户/无限续杯.bat`](普通用户/无限续杯.bat:1) 与 [`客户端/普通用户/无限续杯.sh`](普通用户/无限续杯.sh:1) 的菜单里。
- Windows 版已支持静默定时任务（优先 `conhost --headless`，自动回退 `PowerShell Hidden`）。

用法示例：

```bash
# Windows：先运行“无限续杯”进入菜单，设置配置/定时任务
客户端\\普通用户\\无限续杯.bat

# Windows：单次续杯（可不带参数，读取本地配置）
客户端\\普通用户\\单次续杯.bat

# macOS/Linux：先运行“无限续杯”进入菜单，设置配置/输出 cron 行
bash ./客户端/普通用户/无限续杯.sh

# macOS/Linux：单次续杯（可不带参数，读取本地配置）
bash ./客户端/普通用户/单次续杯.sh
```

---

### 1.1 续杯闭环规则（面向客户端实现与 AI 协作）

以下规则用于约束 [`客户端/普通用户/单次续杯.bat`](普通用户/单次续杯.bat:1) 与 [`客户端/普通用户/无限续杯.bat`](普通用户/无限续杯.bat:1) 的行为，避免“续杯完成但可用性未收敛”。

1) 单次续杯必须支持“迭代到 100% 可用”
- 一次 `topup` 成功并写入本地后，不能立即结束。
- 必须对“本轮新增账号”再次探测可用性。
- 若发现 `401/429`，立即将这些失效项再次上报到 [`/v1/refill/topup`](../server/API.md:1)，请求继续补发。
- 重复“探测 -> 上报 -> 补发 -> 再探测”，直到本轮目标集合中不再出现 `401/429`，才允许收敛完成。

2) sync-all/0 账号场景必须追加续杯校验闭环
- 在 [`客户端/普通用户/单次续杯.bat`](普通用户/单次续杯.bat:1) 中触发 `sync-all`（含本地 0 账号自动切换）后，即使已拿到目标数量（例如 30 个），也不能直接结束。
- 必须再执行一轮“续杯可用性闭环”验证。
- 若仍有 `401/429`，继续迭代补发，直到该批账号达到 100% 可用。

3) 置信回放（replay）账号的二次确认必须“成功/失败都回传”
- 当服务端通过 [`/v1/refill/topup`](../server/API.md:1) 下发带 `replay_from_confidence=true` 的账号时，客户端在复探后必须回传结果。
- 该回传不以是否失败为条件：
  - 成功也要回传（用于服务端做 confirmed/rejected 判定闭环）。
  - 失败（401/429）也要回传（并触发继续补发逻辑）。
- 换言之：普通账号“主要回传失效项”；回放账号“强制回传全部复探结果”。

4) 闭环优先级
- “可用性收敛”优先于“单次请求完成”。
- 任何结束态都应满足：当前目标池内账号经探测后无 `401/429` 未处理残留。

> 备注：本节为客户端执行规则；服务端字段定义与行为以 [`server/src/index.ts`](../server/src/index.ts:3156) 实现为准。

### 1.2 自动清理（仅删 401）

目录：[`客户端/普通用户/自动清理`](普通用户/自动清理:1)

- Windows（手动一键，不需要 Python）：[`客户端/普通用户/自动清理/一键清理_仅删401.bat`](普通用户/自动清理/一键清理_仅删401.bat:1)
- Windows（计划任务/无暂停）：直接给 [`客户端/普通用户/自动清理/一键清理_仅删401.bat`](普通用户/自动清理/一键清理_仅删401.bat:1) 传入 `nopause`（以及可选 `apply`）即可

清理后自动补齐（可选）：
- `apply` 且确实删除文件后，会按配置文件：[`客户端/普通用户/状态/无限续杯配置.env`](普通用户/状态/无限续杯配置.env:1)
  - `TARGET_POOL_SIZE` 目标数量
  - `AUTO_REFILL_AFTER_CLEAN=1` 开启后：若不足配额将自动发起补齐请求
- 补齐请求结果追加写入：[`客户端/普通用户/状态/清理后补齐结果.jsonl`](普通用户/状态/清理后补齐结果.jsonl:1)（来自 `POST /v1/refill/topup` 的原始响应，便于你排查服务端返回）

续杯触发规则（单次续杯脚本内实现）：
- 当一组账号里 `401+429 >= 8`，或当前本地账号数量不足目标配额时，触发 `POST /v1/refill/topup`
- 触发后会删除本地 `status_code ∈ {401,429}` 对应的 json 文件（删除前会在 out/ 里备份）
- 上报给服务端的每个账号状态包含 `status_code`：
  - `401` 表示失效
  - `429` 表示配额用光（服务端可据此延迟一周再放出给其他人用）
- macOS/Linux（手动一键，不需要 Python）：[`客户端/普通用户/自动清理/一键清理_仅删401.sh`](普通用户/自动清理/一键清理_仅删401.sh:1)

说明：
- 默认 DryRun，不会删除；加 `apply` 才会删除。
- 只删除 HTTP 401 的文件，其余一律不删。

---

## 2. 热心群众客户端（上传/上报）

目录：[`客户端/热心群众`](热心群众:1)

热心群众只有一个功能：**提交 JSON**（支持“单文件/文件夹”，并带合规检查）。

- Windows：[`客户端/热心群众/提交JSON.bat`](热心群众/提交JSON.bat:1)
- macOS/Linux：[`客户端/热心群众/提交JSON.sh`](热心群众/提交JSON.sh:1)

用法：

```bash
# 单文件
提交JSON.bat https://127.0.0.1:8787 你的上传密钥 register .\register.json

# 文件夹（提交该目录下所有格式正规的 .json）
提交JSON.bat https://127.0.0.1:8787 你的上传密钥 report .\reports\
```

payload 示例（register）：

```json
{
  "accounts": [
    {
      "email_hash":"<sha256_hex>",
      "account_id":"<可选>",
      "seen_at":"2026-02-28T00:00:00Z",
      "auth_json": {"type":"codex","access_token":"..."}
    }
  ]
}
```

说明：
- `auth_json` 为可选敏感字段，仅当你需要让服务端通过 `POST /v1/refill/topup` 下发账号文件时才上传。
- 服务端会用 `ACCOUNTS_MASTER_KEY_B64` 加密存储；`/admin/backup/export` 仍不会导出任何 token 字段。

payload 示例（probe-report）：

```json
{
  "reports": [
    {"email_hash":"<sha256_hex>","account_id":"<可选>","status_code":401,"probed_at":"2026-02-28T00:00:00Z"}
  ]
}
```

---

## 3. 维修者客户端（维修区处理）

目录：[`客户端/维修者`](维修者:1)

维修者使用 `X-Upload-Key`（与热心群众同凭据体系），但主要调用以下接口：

说明（macOS）：维修者脚本默认不需要安装 `jq`，优先走系统自带 `osascript/JXA` 生成请求体；仅在缺少 osascript 时才会尝试 jq/python3 fallback。

- `POST /v1/repairs/claim`
- `POST /v1/repairs/submit-fixed`
- `POST /v1/repairs/submit-failed`
- `POST /v1/repairs/submit-misreport`

脚本：

- Windows：
  - [`客户端/维修者/维修者_领取维修任务.bat`](维修者/维修者_领取维修任务.bat:1)
  - [`客户端/维修者/维修者_提交修复成功.bat`](维修者/维修者_提交修复成功.bat:1)
  - [`客户端/维修者/维修者_提交修复失败.bat`](维修者/维修者_提交修复失败.bat:1)
  - [`客户端/维修者/维修者_误报恢复.bat`](维修者/维修者_误报恢复.bat:1)
- macOS/Linux：
  - [`客户端/维修者/维修者_领取维修任务.sh`](维修者/维修者_领取维修任务.sh:1)
  - [`客户端/维修者/维修者_提交修复成功.sh`](维修者/维修者_提交修复成功.sh:1)
  - [`客户端/维修者/维修者_提交修复失败.sh`](维修者/维修者_提交修复失败.sh:1)
  - [`客户端/维修者/维修者_误报恢复.sh`](维修者/维修者_误报恢复.sh:1)

---

## 3.1 不同客户端实现说明（网友版本）

目录：

- [`客户端/客户端网友代码1`](客户端网友代码1:1)
- [`客户端/客户端网友代码2`](客户端网友代码2:1)
- [`客户端/客户端网友代码3`](客户端网友代码3:1)
- [`客户端/客户端网友代码4`](客户端网友代码4:1)

用途说明：

- 以上目录用于历史方案对照、功能借鉴与迁移参考。
- 当前推荐生产使用路径仍为：[`客户端/普通用户`](普通用户:1)

---

## 4. 超级管理员客户端（管理）

目录：[`客户端/超级管理员`](超级管理员:1)

- 查看统计：
  - Windows：[`客户端/超级管理员/查看统计.bat`](超级管理员/查看统计.bat:1)
  - macOS/Linux：[`客户端/超级管理员/查看统计.sh`](超级管理员/查看统计.sh:1)

- 生成用户密钥（服务端自动生成，返回一次明文）：
  - Windows：[`客户端/超级管理员/生成用户密钥_申请.bat`](超级管理员/生成用户密钥_申请.bat:1)
  - macOS/Linux：[`客户端/超级管理员/生成用户密钥_申请.sh`](超级管理员/生成用户密钥_申请.sh:1)

- 生成上传密钥（服务端自动生成，返回一次明文）：
  - Windows：[`客户端/超级管理员/生成上传密钥_申请.bat`](超级管理员/生成上传密钥_申请.bat:1)
  - macOS/Linux：[`客户端/超级管理员/生成上传密钥_申请.sh`](超级管理员/生成上传密钥_申请.sh:1)

- 备份有效数据（导出到本地 JSON，不含日志/无效库/任何 token 字段）：
  - Windows：[`客户端/超级管理员/导出有效数据_备份.bat`](超级管理员/导出有效数据_备份.bat:1)
  - macOS/Linux：[`客户端/超级管理员/导出有效数据_备份.sh`](超级管理员/导出有效数据_备份.sh:1)

- 恢复有效数据（从导出的 JSON 恢复）：
  - Windows：[`客户端/超级管理员/导入有效数据_恢复.bat`](超级管理员/导入有效数据_恢复.bat:1)
  - macOS/Linux：[`客户端/超级管理员/导入有效数据_恢复.sh`](超级管理员/导入有效数据_恢复.sh:1)

> 旧的“导入用户/上传密钥”脚本仍保留用于历史兼容，但在“服务端自动生成密钥”的新流程下不再推荐使用。
