# Antigravity (Gemini Code Assist) 配置
# ================================
# 通常不需要手动配置，认证后 Token 会自动保存到:
#   Windows: %USERPROFILE%\.nl_llm\antigravity_token.json
#   Linux/macOS: ~/.nl_llm/antigravity_token.json
#
# 如需指定其他 Token 路径，可通过命令行参数传入:
#   test_antigravity_auth.exe C:\path\to\token.json

# 默认使用的模型
MODEL=gemini-2.5-flash

# 默认测试提示词
PROMPT=Hello from Antigravity!
